<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>fastcms-plugins</artifactId>
        <groupId>com.fastcms</groupId>
        <version>${revision}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>twplus-components-plugin</artifactId>
    <description>AI 模板组件包扩充插件（twx）：10 个内容区组件，PF4J 插件方式供给</description>

    <properties>
        <plugin.id>twplus-components-plugin</plugin.id>
        <plugin.class>com.fastcms.twplus.TwPlusPlugin</plugin.class>
        <plugin.version>0.0.1</plugin.version>
        <plugin.provider>wjun_java@163.com</plugin.provider>
        <plugin.description>AI template component pack: 10 content-area sections</plugin.description>
        <plugin.dependencies />
    </properties>

    <dependencies>

        <dependency>
            <groupId>com.fastcms</groupId>
            <artifactId>fastcms-ai-starter</artifactId>
            <version>${revision}</version>
            <scope>provided</scope>
        </dependency>

    </dependencies>

    <build>
        <!-- 组件包资源（component.json / variants/*.ftl / static/pack.css）必须完整打入插件 jar：
             父 pom 的 resources 过滤只保留 html/sql，此处整体覆盖 -->
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>false</filtering>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>2.4</version>
                <configuration>
                    <archive>
                        <manifestEntries>
                            <Plugin-Id>${plugin.id}</Plugin-Id>
                            <Plugin-Class>${plugin.class}</Plugin-Class>
                            <Plugin-Version>${plugin.version}</Plugin-Version>
                            <Plugin-Provider>${plugin.provider}</Plugin-Provider>
                            <Plugin-description>${plugin.description}</Plugin-description>
                            <Plugin-Dependencies>${plugin.dependencies}</Plugin-Dependencies>
                        </manifestEntries>
                    </archive>
                </configuration>
            </plugin>

            <plugin>
                <artifactId>maven-deploy-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>

        </plugins>
    </build>

</project>
